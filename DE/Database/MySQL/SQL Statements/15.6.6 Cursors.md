[15.6.6.1 Cursor CLOSE Statement](https://dev.mysql.com/doc/refman/8.4/en/close.html)

[15.6.6.2 Cursor DECLARE Statement](https://dev.mysql.com/doc/refman/8.4/en/declare-cursor.html)

[15.6.6.3 Cursor FETCH Statement](https://dev.mysql.com/doc/refman/8.4/en/fetch.html)

[15.6.6.4 Cursor OPEN Statement](https://dev.mysql.com/doc/refman/8.4/en/open.html)

[15.6.6.5 Restrictions on Server-Side Cursors](https://dev.mysql.com/doc/refman/8.4/en/cursor-restrictions.html)

MySQL hỗ trợ con trỏ bên trong các chương trình lưu trữ. Cú pháp tương tự như trong SQL nhúng. Con trỏ có các thuộc tính sau:

- Asensitive: Máy chủ có thể sao chép hoặc không sao chép bảng kết quả của nó.
- Chỉ đọc: Không thể cập nhật
- Không thể cuộn: Chỉ có thể di chuyển theo một hướng và không thể bỏ qua hàng.

Khai báo con trỏ phải xuất hiện trước khai báo hàm xử lý và sau khai báo biến và điều kiện.

Ví dụ:

```sql
CREATE PROCEDURE curdemo()
BEGIN
  DECLARE done INT DEFAULT FALSE;
  DECLARE a CHAR(16);
  DECLARE b, c INT;
  DECLARE cur1 CURSOR FOR SELECT id,data FROM test.t1;
  DECLARE cur2 CURSOR FOR SELECT i FROM test.t2;
  DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;

  OPEN cur1;
  OPEN cur2;

  read_loop: LOOP
    FETCH cur1 INTO a, b;
    FETCH cur2 INTO c;
    IF done THEN
      LEAVE read_loop;
    END IF;
    IF b < c THEN
      INSERT INTO test.t3 VALUES (a,b);
    ELSE
      INSERT INTO test.t3 VALUES (a,c);
    END IF;
  END LOOP;

  CLOSE cur1;
  CLOSE cur2;
END;
```

Nguồn: [[MySQL  MySQL 8.4 Reference Manual  15.6.6 Cursors]]
